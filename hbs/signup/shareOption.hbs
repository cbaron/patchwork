<div data-js="container" class="share-option col-sm-3">
    <div>{{label}}</div>
    <select data-js="input" class="form-control">
        {{#each options}}
            <option value="{{id}}">{{label}}</option>
        {{/each}}
    </select>    
</div>
