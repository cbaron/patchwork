<form class="form-horizontal add-checklist-item">
    {{#each field}}
        <div class="form-group">
            <label for="{{name}}" class="col-sm-3 control-label">{{name}}</label>
            <div class="col-sm-9">
                <input type="text" id="{{name}}"/>
            </div>
        </div>
    {{/each}}
</form>
