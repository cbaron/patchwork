CREATE TABLE internshipduty (
    id             SERIAL PRIMARY KEY,
    name           VARCHAR(100),
    text           VARCHAR(100)
);

CREATE TABLE internshipqualification (
    id             SERIAL PRIMARY KEY,
    name           VARCHAR(100),
    text           VARCHAR(100)
);